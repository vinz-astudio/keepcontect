# 0.7.0 passive check-in release readiness

Status: **Stage 1 deployed to production 2026-08-15 (shadow-only, all accounts
legacy); live activation and artifact publication remain unauthorized**.
Gates 2-6 open; next human checkpoint is the post-14-day-shadow review.

Source commit: `b8959b1b04e6e79525a4fbd3cf666a01c015447e` on
`codex/passive-checkin-impl`. Existing accounts remain `legacy`; no remote
Supabase command, Edge deployment, Vercel deployment, GitHub publication,
Store submission, TestFlight upload or account rollout was run.

## Passed locally

- `npm run local:gate`: version truth `0.7.0`, TypeScript, 458 Vitest tests,
  Web build and Brain SDLC check passed. The only warning is the absent Tauri
  release signing key.
- `npm run db:replay:compat`: the folded production baseline plus every live
  migration replayed; 47 pgTAP files / 1106 tests passed.
- Tauri: 6 Rust tests passed; NSIS and MSI builds completed.
- Android: `testReleaseUnitTest`, `assembleRelease`, `bundleRelease` passed.
  APK metadata is `com.keepcontact.app`, `versionName=0.7.0`,
  `versionCode=87`, target SDK 35. APK v1/v2 verification and AAB JAR
  verification passed with the existing release certificate.
- Capacitor Android and iOS assets exactly match the current Web entry bundle:
  `D6FBA98BE26CBC8B6FB313B2723C9A4B20B81138D18D21598B452D28907A937B`.
- `npx cap sync ios` passed and found the KC passive plugin. CocoaPods and
  Xcode are unavailable on this Windows machine, so this is a static/sync
  result, not an iOS build result.

## Candidate artifacts

| Artifact | Bytes | SHA-256 | State |
|---|---:|---|---|
| `release-artifacts/keep-contact-v0.7.0.apk` | 5,388,261 | `53E93C594F1F327761B1BED0C4B08AE72BAB3CD4F9835527AE8039A1BD765658` | signed local candidate |
| `release-artifacts/keep-contact-v0.7.0.aab` | 4,987,705 | `89ED5E0C8186891D0BD7B8B9C5E95FE8977D63BA4CBC738C309D75590F9974DC` | signed local Play candidate |
| `%LOCALAPPDATA%/Temp/kc-passive-070-tauri-target/release/bundle/nsis/Keep Contact_0.7.0_x64-setup.exe` | 3,611,618 | `4C05E29BD9B86797B087BA50EA29637568BD797113956FD2BEE590871C2AABD6` | built, not release-signed |
| `%LOCALAPPDATA%/Temp/kc-passive-070-tauri-target/release/bundle/msi/Keep Contact_0.7.0_x64_en-US.msi` | 4,771,840 | `57DCB948193732852233CCE6F8CB6DEFDDDB9A5F767AA2CF5F1AF2183F428D2A` | built, not release-signed |

The Android stable aliases were updated locally to the same bytes. The
desktop public aliases were deliberately not replaced by unsigned builds.

## Open gates — all remaining are blocking

- [x] **DONE 2026-08-15** (guarded task `KC-RELEASE-0.7.0-STAGE1-001` completed):
      six Stage 1 migrations (`20260814183000`, `20260814190000`,
      `20260814193000`, `20260814200000`, `20260814203000`,
      `20260814225000`) deployed in timestamp order plus
      `20260813120000`/`20260814140000` (branch migrations),
      `supabase/functions/passive-evidence` deployed (ACTIVE v1).
      Verified live: dry-run `upToDate:true`; ledger rows for all eight,
      `20260814201500` absent; `globally_disabled=false`, 0 accounts,
      `activate_passive_checkin_contract` count 0; ingest RPC execute
      anon=false/authenticated=false/service_role=true. Independent Opus 5
      audit PASS (D01-R3, 0 blockers); local replay 46 files / 1090 tests
      PASS. `20260814201500` (activation) stays deferred to the post-gate
      checkpoint (audit BLK-3). `20260814193000` carries the declared
      non-additive replacements with forward restore
      (`docs/release/0.7.0-restore-legacy-escalation.sql`) and a
      kill-switch-gated, containment-wrapped evaluator (BLK-1/BLK-2).
- Obtain an independent non-author review with no blocker.
- Run at least 14 consecutive real shadow days. Every platform/contract/client
  cohort must contain at least 20 qualifying accounts, median interruptions
  must be `<=0.2/user/day`, p90 `<=1/user/day`, every hard-invariant counter
  must be zero, causal replay must explain every outcome, and late-positive
  false-alert segmentation must be reviewed. Evaluate the aggregate JSON with
  `npm run passive:shadow:report -- <report.json>`.
- Android real-device/OEM gates: Doze, offline/reconnect, reboot, permission
  revocation, old-client behavior and notification escalation. Complete the
  Play `specialUse`, data-safety and `PACKAGE_USAGE_STATS` prominent-disclosure
  material; verify concern notification behavior without full-screen intent.
- iOS: Xcode archive, CocoaPods, TestFlight and at least three devices,
  including terminated/force-quit measurement and permission revocation.
- Desktop: build with the authorized release signing key, verify the produced
  signature/update path, then test Windows and a signed macOS build on device.
- Human authorization is still required separately for production mutation,
  public artifact publication, `app_versions` registration, Vercel deployment,
  TestFlight/Play submission and any live account migration.

## Production order after authorization

1. Deploy migrations `20260814183000`, `20260814190000`, `20260814193000`,
   `20260814200000`, `20260814203000`, `20260814225000` in order.
   `20260814201500` (activation) stays out of Stage 1 — it carries end-user
   activation authority and its own 14-day gate, and is deferred to the
   post-shadow-gate checkpoint (audit BLK-3, 2026-08-14).
2. Deploy `passive-evidence`; verify scheduled jobs, private ACLs, rejection
   telemetry and that all existing accounts remain `legacy`.
3. Publish compatible collectors/clients, then let users explicitly save a
   contract into `shadow`. Do not activate live authority.
4. Complete the real 14-day/cohort/device/review gates and retain the aggregate
   report as release evidence.
5. After a new human release checkpoint, make signed artifacts reachable
   first. Register `app_versions` and publish `version.json` last so no client
   is offered a missing or unsigned download.
6. Only users who explicitly confirm the migration may call the live
   activation RPC. Users who do nothing stay `legacy`.

Rollback procedure: `0.7.0-passive-checkin-rollback.md`.
