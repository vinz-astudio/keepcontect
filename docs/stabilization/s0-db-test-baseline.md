# S0 DB Test Baseline — Triage Cluster 1–4/4

Read-only triage of the seventeen failing pgTAP files named below from the captured S0 DB test run. No SQL or migration was edited. This report covers **17 of the 17 failing files**.

## Command & Totals

- Command: `npm exec --package=supabase@2.109.1 -- supabase test db`
- Files: **26** · Tests: **696** · Result: **FAIL** · 25 wallclock secs
- Failing files: **17** · Passing files: 9
- Runner tail: `error running container: exit 1` · `DB_TEST_EXIT=1`
- Isolated cluster-2 repro (2026-08-09, `--local`): `npm exec --package=supabase@2.109.1 -- supabase test db --local supabase/tests/adaptive_alert_candidate_evaluator.sql supabase/tests/adaptive_alert_gap_profiles.sql supabase/tests/adaptive_alert_routine_modes.sql supabase/tests/routine_mode_cohort_priors.sql` — 4 files, 193 tests, FAIL exit 1; the cluster-2 failures reproduce outside the full 26-file run, so they are not full-suite-only ordering effects
- Isolated cluster-3 repro (2026-08-09, `--local`): all four files added below reproduced outside the full 26-file run; a larger local-only command covering 9 remaining files (302 tests) exited 1 — cluster-3 failures are stable and not full-suite-only ordering effects
- Isolated cluster-4 repro (2026-08-09, `--local`): all five files added below reproduced under explicit `supabase test db --local` as part of the 9-file/302-test remaining-cluster run (FAIL exit 1) — cluster-4 failures reproduce outside full-suite run
- Report coverage: **17/17** failing files
- Status: **not green** — this report must not be read as a passing suite.
- Zero production actions: no git push/tag, deployment, release, remote database execution, or hosted-environment operation was run to produce this report.
- Redaction confirmation: no personal identifiers, passwords, API tokens, secrets, credential-bearing URLs, or production row data appear in this report or in the evidence inputs used for it.

## Triage Table

| File | Failing signature / count | Class | Evidence paths / functions | Confidence | Smallest S2 follow-up |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `supabase/tests/account_threshold_shadow.sql` | 12/15 — test 2 dies `23502: null value in column "live_threshold_minutes" ... violates not-null constraint`; tests 3–5, 7–14 `have: NULL` vs expected values | needs Manager decision | `supabase/migrations/20260808160000_baseline_from_production.sql`: `private.silence_threshold(uuid)` (line 6717) returns NULL unless an `account_normal_bounds` row has `has_usable_signal AND lookback_days=30 AND false_alarm_budget=1`; `private.record_account_threshold_shadow` (line 4151, CTE ~4188) computes `round(extract(epoch FROM private.silence_threshold(user_id))/60)::integer` → NULL → NOT NULL death; definition pin `private.shadow_live_definition_matches` (~6655) authorizes `6be4ed54…` while live LF `pg_get_functiondef` hash is `c3efc6cc…` | High on root cause (silence_threshold NULL); hash drift is separate definition-divergence evidence | Manager arbitrates the authorized `silence_threshold` definition (re-apply ADR-authorized body vs. change the expectation), then re-run this file |
| `supabase/tests/history_seeded_live_threshold_and_sleep.sql` | 6/10 — tests 1–6 all `private.silence_threshold(...)` `have: NULL` vs `04:55:00 / 08:00:00 / 04:10:00 / 01:30:00 / 01:30:00 / 10:00:00` | needs Manager decision | Test seeds `alert_gap_profiles` personal history, a shadow `alert_model_versions` row, enables `private.adaptive_alert_shadow_runtime_config` (version_id, `enabled=true`, `accept_coverage_leases=true`) and `user_settings`; `private.silence_threshold` reads none of them — only `account_normal_bounds` — so NULL. Either history-seeded live thresholds are not the wired contract (obsolete expectation / NULL-by-design) or the history path was never connected (defect). Same migration evidence as above | High on root cause; which side is correct is the open question | Manager decides whether history-seeded thresholds are the live contract, then either rewrites the fixture or wires `silence_threshold` to the history model; re-run |
| `supabase/tests/gm_mute_user.sql` | 2/13 — test 4 `has_table_privilege('authenticated','public.gm_mutes','SELECT')` `have: true want: false`; test 13 live hash `have: c3efc6cc… want: 6be4ed54…` | probable product defect | `GRANT ALL ON public.gm_mutes TO anon, authenticated, service_role` (migration lines 11740–42); RLS enabled (line 10952); no `CREATE POLICY` matches `gm_mutes` (verified) → capability grant is over-broad vs. the test's "narrow explicit live-authority exception" intent (RLS still blocks row reads). Test 13 pins `6be4ed54…` at `gm_mute_user.sql` line 249 via `private.shadow_live_definition_matches` (~6655) | Medium-High | Revoke `anon`/`authenticated` grants on `public.gm_mutes` or add a purpose-built RLS policy (product fix); Manager resolves the authorized hash before re-run |
| `supabase/tests/routine_safety.sql` | 7/35 — test 20 `task_missed` EXISTS false; test 24 alert `have: (open) want: (resolved)`; tests 30–33 `have: ()` vs `(1.50)/(2.25)/(3.00)/(3.00)`; test 35 `have: ()` vs `(10800)` | needs Manager decision | Threshold cluster (30–33, 35) is the same `silence_threshold` NULL root, surfaced via `round(extract(epoch FROM private.silence_threshold(...))…)` and `my_routine_status()` (line 7669). Test 24: `apply_liveness_side_effects` (line 315) only updates `device_state` and conditionally deletes self/concern notifications — no path auto-resolves an open alert on a live ping (resolution only in the sleep-grace resolver ~7880, `resolve_alert` 8369, `resolve_my_alert` 8406) → obsolete-leaning but needs ADR check. Test 20: `process_checkin_tasks()` (line 7766; loop-2 `_done` EXISTS ~7816–25, recipient resolution ~7833–51) reads correct on inspection → root cause not pinned, isolated repro required | Medium-High overall; Medium on tests 20/24 | Manager decides threshold and live auto-resolve semantics; then an isolated repro of the test-20 `task_missed` path; re-run |
| `supabase/tests/adaptive_alert_candidate_evaluator.sql` | 5/79 — test 13 `alert_judgment_subject_contexts` exposes `MAINTAIN` to `anon`; test 68 cohort source-generation returned `routine_cohort` vs expected `deterministic_emergency`; test 69 configured pre-promotion live-definition pin `have: false`; test 70 emergency balanced threshold `have: NULL` vs 135 (90/135/180 contract); test 74 `process_escalations()` live hash `have: dae555ee… want: fde0f2ec…` (also pins `silence_threshold` to `6be4ed54…`) | needs Manager decision | Test lines 26–89 (ACL), 1765–1807 (source generation, pin, emergency threshold), 2007–2015 (live hashes); `supabase/migrations/20260808160000_baseline_from_production.sql`: `private.resolve_alert_candidate(uuid,timestamptz,uuid)` (lines 5182–5902), `private.silence_threshold(uuid)` (~6717), `private.shadow_live_definition_matches` (~6655) | High on test 13 ACL mismatch; Medium on tests 68–70, 74 — contract/hash/threshold divergence kept as mixed evidence | Manager arbitrates the authorized `resolve_alert_candidate` routing, emergency-threshold contract, and `fde0f2ec…` hash pin; then re-run this file |
| `supabase/tests/adaptive_alert_gap_profiles.sql` | 1/57 — test 55: `alert_intervention_events` exposes `MAINTAIN` to `anon`; expected all eight table actions (DELETE, INSERT, MAINTAIN, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE) denied to every Data API role | probable product defect | Test lines 162–168 enumerate the eight actions; `supabase/migrations/20260808160000_baseline_from_production.sql` privilege-recovery block lines 11890–12025 — for `alert_intervention_events` lines 11936–11944 revoke only REFERENCES/TRIGGER/TRUNCATE, with no `MAINTAIN` revoke | High — stable local-only ACL mismatch in the canonical baseline, reproduced in the isolated 4-file run | Add the missing `MAINTAIN` revoke for `alert_intervention_events` in the privilege block (product fix); then re-run this file |
| `supabase/tests/adaptive_alert_routine_modes.sql` | 3/15 — test 11 `routine_mode_cohort_invalidations` exposes `MAINTAIN` to `anon`; test 14 update to `semester_break` produced no invalidation rows (expected `regular_9to5` + `semester_break`); test 15 consent update produced no row (expected `(semester_break,true)`) | needs Manager decision | Test lines 26–83 (ACL), 101–139 (insert/update/expected invalidations); `private.invalidate_routine_mode_cohort()` (lines 1758–1811); trigger `on_profile_routine_mode_cohort_invalidation` (line 10449); generation seed (12049–12051); privilege block (12008–12016) revokes only REFERENCES/TRIGGER/TRUNCATE, not MAINTAIN | High on test 11 ACL mismatch; Medium on tests 14–15 — trigger and seed present on inspection yet no rows, cause not pinned | Isolated SQL repro recording generation-table state and trigger firing before changing the contract; Manager decides the test-11 MAINTAIN revoke; re-run |
| `supabase/tests/routine_mode_cohort_priors.sql` | 15/42 — tests 9–11, 17–18, 23, 25–27, 29, 31–35; initial weighted/semester/trimmed rebuilds report `published: 0` vs expected 1; aggregate rows consequently NULL/missing; invalidation assertions fail (profile-key changes do not increment generations; withdrawal/mode-change checks return NULL or zero deltas) | needs Manager decision | Fixture/config lines 4–82; first rebuild assertions 93–109; invalidation/rebuild assertions 116–150; `private.rebuild_routine_mode_cohort_priors` (lines 3904–4148) returns `published: 0` on missing/invalid model/generation/config (3965–4001), zero qualifying profiles (4035–4051), or no changed upsert; invalidation functions (1758–1859); generation seed (12049–12051) | High on stability (reproduces in the isolated 4-file run); Medium on root cause — first failing builder guard not pinned | Single-file diagnostic repro of the earliest `published: 0` guard and generation state before any test or function edit; re-run |
| `supabase/tests/adaptive_alert_shadow_coverage_contract.sql` | 1/38 — test 4: "Phase-2 runtime is enabled and bounded" `have: (f,f,10000,35,120,3,0)` vs `want: (t,t,10000,35,120,3,0)` for enabled, accept_coverage_leases and bounds | needs Manager decision | Test lines 84–89; migration lines 8979–8988: runtime table defaults `enabled=false`, `accept_coverage_leases=false`. Baseline system-row comments say `alert_model_versions` is deliberately not seeded and dispatcher returns early while version_id is null (12042–12047); row insert supplies only singleton (12053–12055) | High on root cause, failure is stable and baseline intent explicit; whether S2 should preserve disabled-safe bootstrap or restore old Phase-2-enabled product state is open | Manager decides whether S2 should preserve disabled-safe bootstrap or restore old Phase-2-enabled product state; re-run |
| `supabase/tests/adaptive_alert_shadow_operational_cycle.sql` | 1/46 — test SQL line 115 `ERROR: shadow_live_hash_mismatch` from `private.run_adaptive_alert_shadow_cycle` line 60 (planned 46, ran 21, exit 3) | needs Manager decision | `private.run_adaptive_alert_shadow_cycle` (migration lines 6031–6095) validates configured emergency expected hash against `pg_get_functiondef(private.silence_threshold)` and raises at line 6094 | High — deterministic live-definition divergence, not test flakiness | Manager arbitrates authorized hash/body for `silence_threshold` (same arbitration as cluster 1); re-run |
| `supabase/tests/adaptive_alert_shadow_operational_schema.sql` | 4/42 — tests 34–36, 40; test 34 sensitivity remained `balanced` (want `low`), tests 35–36 `unreplayable_reason` NULL (want `invalid_timezone` and `future_source_timestamp`), test 40 consent withdrawal did not create `private.adaptive_alert_shadow_cohort_dirty` row | test isolation/flakiness | Test lines 146–160 (test 34), 162–180 (tests 35–36), 203–207 (test 40); `private.capture_alert_shadow_subject_contexts` (migration lines 864–1101) reads monitored population, settings/timezone/profile data and stores replayability state | Medium on root cause — exact failing branch inside `capture_alert_shadow_subject_contexts` is not pinned from evidence | Require single-file diagnostic repro before changing tests/functions |
| `supabase/tests/adaptive_alert_shadow_recorder.sql` | 1/67 — test 16: "Task 8 leaves ADR-0022 live threshold and Guardian 30-minute state machine definitions unchanged", `process_escalations()` live hash `have: dae555ee… want: fde0f2ec…` (also pins `silence_threshold` to `6be4ed54…`) | needs Manager decision | Test lines 342–347; definition divergence between live definition and ADR pin; same definition drift as clusters 1–2 | High on root cause — definition divergence between live definition and ADR pin | Manager resolves authorized live function definition; re-run |
| `supabase/tests/adaptive_alert_shadow_schema.sql` | 3/63 — test 47 `MAINTAIN` exposed to `anon` on `alert_gap_profiles`; test 59 cron list includes `account-shadow-cycle-v1` at `37 2 * * *` vs expected only two shadow jobs; test 60 model-version count 0 vs expected 1 history model | needs Manager decision | Test lines 157–162, 281–286, 304–307; `supabase/migrations/20260808160000_baseline_from_production.sql` lines 11879–11881 (cron schedule), 12042–12047 (deliberately unseeded alert_model_versions) | High — mixed ACL defect signal plus explicit baseline/product-state conflicts | Manager resolves ACL subfinding and baseline/product-state schedule and seeding contracts; re-run |
| `supabase/tests/adaptive_shadow_history_seeded_activation.sql` | 6/15 — tests 2–4, 7, 11, 15; no seeded history model (0 vs 1); NULL model evidence policy and runtime model name; live threshold hash divergence `c3efc6cc…` vs `6be4ed54…`; profile `(t,4,f,f)` vs expected `(t,5,t,t)`; no initial validation-cycle row | needs Manager decision | Test lines 469–474 (initial validation cycle for `kc-shadow-prod-v2-history-seeded`); `supabase/migrations/20260808160000_baseline_from_production.sql` lines 12042–12055 (deliberately omitted model seed/runtime version); shared hash divergence | High — failures combine deliberately absent activation data with live definition divergence and downstream non-activation | Manager arbitrates model seeding/activation contract vs baseline omit decision; re-run |
| `supabase/tests/adaptive_shadow_live_hash_canonicalization.sql` | 2/5 — test 2 dies `P0001: LF production representation was rejected`; test 5 normalized live threshold hash `c3efc6cc…` vs `6be4ed54…` | needs Manager decision | `private.shadow_live_definition_matches` gate; CRLF/LF normalization does not explain the current semantic/body hash divergence | High — deterministic definition conflict requiring authorized-body/hash arbitration | Manager arbitrates authorized body and canonical hash representation; re-run |
| `supabase/tests/adaptive_shadow_subject_context_provenance.sql` | 3/6 — test 1 dies `invalid shadow context capture arguments`; test 3 returns `invalid_version_status` vs expected `missing_qualified_session`; test 4 canonical sensitivity row NULL vs `balanced` | needs Manager decision | Test 4 lines 150–155; `capture_alert_shadow_subject_contexts` (migration lines 883–887, 889–900) rejects NULL args and requires valid enabled shadow version; baseline (lines 12042–12055) deliberately omits model seed/runtime version | High — downstream of absent activation/version input, fixture vs baseline activation contract choice | Manager decides whether fixture setup or baseline activation contract governs; re-run |
| `supabase/tests/adaptive_sleep_candidate.sql` | 1/45 — test 21 `alert_sleep_night_contexts` exposes `MAINTAIN` to `anon`; expected no effective table action for Data API roles | probable product defect | Test lines 120–125 (enumerates DELETE, INSERT, MAINTAIN, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE); `supabase/migrations/20260808160000_baseline_from_production.sql` privilege block lines 11990–11998 revokes only REFERENCES/TRIGGER/TRUNCATE from anon/authenticated/service_role | High — stable local ACL mismatch in canonical baseline | Add missing `MAINTAIN` revoke for `alert_sleep_night_contexts` in privilege block (product fix); re-run |

## Notes on Mixed Sub-Classes

- `gm_mute_user.sql` is classed **probable product defect** on the strength of test 4 (over-broad grants, a real ACL mismatch). Test 13 is not a mute defect — it is the same live-definition hash drift seen across cluster 1 and belongs to the Manager arbitration track.
- `routine_safety.sql` is classed **needs Manager decision** because 5 of 7 failures are threshold-NULL (a contract question, not an isolated bug), test 24 leans obsolete but is unconfirmed, and test 20 leans defect but lacks a pinned root cause. None of the three sub-classes is settled enough to relabel the file.
- `adaptive_alert_candidate_evaluator.sql` is classed **needs Manager decision** because it mixes an ACL defect signal (test 13, `MAINTAIN` to `anon`) with live-contract/hash/threshold divergence (tests 68–70, 74); the evidence pack explicitly says not to collapse the row to a single unqualified defect.
- `adaptive_alert_routine_modes.sql` is classed **needs Manager decision** because tests 14–15 fail with the trigger and generation seed present on inspection and no pinned trigger-path cause; test 11 is a probable ACL defect but is only 1 of 3 failures.
- `routine_mode_cohort_priors.sql` is classed **needs Manager decision** because the failure is stable in the isolated run but the first failing `published: 0` guard inside the builder is not pinned; uncertainty is preserved pending a single-file diagnostic repro.
- Hash mismatches (`have: c3efc6cc… want: 6be4ed54…`) are presented as triage evidence of definition divergence only. This report does not verdict which definition is correct; the ADR-authorized pin and the live body disagree, which is a logical/rules conflict for the human/Manager to arbitrate per AGENTS.md.

## Scope & Honesty

- Only the seventeen files named in the table were inspected, plus the single active baseline migration (`supabase/migrations/20260808160000_baseline_from_production.sql`) and the referenced tests.
- Cluster-2, cluster-3, and cluster-4 rows are based on their respective deterministic evidence packs and local isolated reproductions; no repository source, raw log, network/database/production target, or extraneous files were inspected.
- No code or SQL was changed. No production action was taken. The suite remains **not green** (`DB_TEST_EXIT=1`).
