# KC S0 Truth Convergence Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Converge the current main-line Android/design work and Claude's six database commits into one isolated, auditable local source baseline, while making zero production or release changes.

**Architecture:** Work only in a dedicated git worktree based on the execution-time local `main`, which must contain design commit `37d78ed` and this committed plan. Record that resolved SHA as `S0_BASE` before creating the worktree. Merge the complete Claude database branch without rewriting its history, then establish mechanical web/Android/database baselines. agy produces the source-inventory artifact; DeepSeek V4 classifies the database-test failures. Codex reviews semantics, integrates results, updates Brain truth, and owns acceptance.

**Tech Stack:** Git worktrees, React 19, Vite 6, TypeScript 5.8, Vitest 3, Capacitor Android 7, Gradle, Supabase CLI 2.109.1/Postgres pgTAP, Obsidian Brain guarded-task runtime.

## Global Constraints

- Production remains untouched. No deployment, release, version bump, tag, push, linked Supabase mutation, remote migration, or hosted-environment command is authorized.
- All source changes occur in `C:\Users\vizen\Desktop\GCIT Tasks\Keep contact-worktrees\kc-stabilization-s0`, branch `codex/kc-stabilization-s0`.
- Preserve both source histories. Merge `claude/upbeat-panini-7a75aa@693e71b` with a merge commit; do not cherry-pick, squash, rebase, or edit old migrations.
- Database verification uses only the local Supabase stack. A red database suite is expected at S0; the acceptance condition is an exact, reviewed baseline, not artificial greening.
- Executor orders are hash-addressed, write-set-limited, and non-overlapping. agy and V4 may write only their assigned Markdown report. They may not edit code, migrations, tests, Brain notes, or git history.
- Reports must redact personal identifiers, secrets, tokens, URLs containing credentials, and production row data.
- If any precondition, merge result, or baseline differs materially from this plan, stop and return to Codex Manager. Executors do not fill gaps.
- Local rollback is abandoning the isolated branch/worktree. Never reset or clean the user's root worktree destructively.

---

## Task 1: Open the guarded S0 implementation task

**Brain files:**

- Create: `Coordination/Routes/KC-STABILIZATION-S0-IMPL-001.task-card.json`
- Generated by runtime: guarded Task Context and Active Work entry

- [ ] Create the task card with external write set limited to the isolated worktree and these Brain truth notes:
  - `Coordination/Status/Keep Contact Status.md`
  - `Projects/Keep Contact/Keep Contact.md`
  - `Projects/Keep Contact/Business Logic/Alert System.md`
  - `Projects/Keep Contact/Business Logic/Behavior Pings.md`
  - `Projects/Keep Contact/Business Logic/Notification Flow.md`
  - `Projects/Keep Contact/Business Logic/Guardian Confirm.md`
  - `Projects/Keep Contact/Business Logic/Google Play Store Compliance.md`
  - `Projects/Keep Contact/iOS Native/iOS Native TestFlight Pipeline.md`
  - `Projects/Keep Contact/Known Issues.md`
  - `Experiences/Keep Contact/Dev Log.md`
- [ ] Lock exactly two executor orders: agy owns `docs/stabilization/s0-source-inventory.md`; V4 owns `docs/stabilization/s0-db-test-baseline.md`.
- [ ] Start the task and run the Brain runtime check.
- [ ] Read the generated Task Context completely before changing the repo.

Expected: task state is `in_progress`; runtime check passes; executor orders have fixed hashes and disjoint write sets.

## Task 2: Create and verify the isolated worktree

**Files:** none

- [ ] Verify the root and Claude source worktrees are clean:

  ```powershell
  git -C 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact' status --short
  git -C 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact\.claude\worktrees\upbeat-panini-7a75aa' status --short
  ```

  Expected: both commands print nothing.

- [ ] Verify immutable source refs and capture the current local plan-bearing main SHA:

  ```powershell
  $s0Base = git -C 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact' rev-parse main
  git -C 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact' merge-base --is-ancestor 37d78ed $s0Base
  git -C 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact' show "$s0Base`:docs/superpowers/plans/2026-08-09-kc-s0-truth-convergence.md" > $null
  $s0Base
  git -C 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact' rev-parse claude/upbeat-panini-7a75aa
  git -C 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact' merge-base main claude/upbeat-panini-7a75aa
  ```

  Expected: both plan-bearing checks exit 0; `$s0Base` is recorded in guarded evidence; Claude is `693e71b73f561d5da9742c108f51af833363a074`; merge base is `666e09ba2ad0ac5a2f9750cf45a259049cfd62b0`.

- [ ] Confirm the target path and branch do not already exist, then create the worktree:

  ```powershell
  Test-Path -LiteralPath 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact-worktrees\kc-stabilization-s0'
  git -C 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact' show-ref --verify --quiet refs/heads/codex/kc-stabilization-s0
  git -C 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact' worktree add -b codex/kc-stabilization-s0 'C:\Users\vizen\Desktop\GCIT Tasks\Keep contact-worktrees\kc-stabilization-s0' main
  ```

  Expected: `False`; `show-ref` exits 1; worktree creation succeeds at `37d78ed`.

- [ ] Run `npm ci` in the isolated worktree.

Expected: dependency installation succeeds without editing tracked files.

## Task 3: Merge Claude's complete database history

**Files:** merge result only; do not hand-edit during this task

- [ ] Preview the merge and confirm no conflict markers are reported:

  ```powershell
  git merge-tree (git merge-base HEAD claude/upbeat-panini-7a75aa) HEAD claude/upbeat-panini-7a75aa
  ```

- [ ] Merge with preserved ancestry:

  ```powershell
  git merge --no-ff claude/upbeat-panini-7a75aa -m 'merge: converge Claude database baseline'
  ```

- [ ] Verify all six Claude commits are ancestors of `HEAD`:

  ```powershell
  @('84959ec','9f239a1','3d05893','bd276bb','85abaf5','693e71b') | ForEach-Object { git merge-base --is-ancestor $_ HEAD; if ($LASTEXITCODE -ne 0) { throw "Missing ancestor $_" } }
  ```

- [ ] Verify the main-side design commit remains an ancestor:

  ```powershell
  git merge-base --is-ancestor 37d78ed HEAD
  ```

Expected: clean merge commit; all ancestry checks exit 0; no manual conflict resolution occurred.

## Task 4: Establish the local web and Android baseline

**Files:** none expected

- [ ] Run TypeScript checking:

  ```powershell
  npm run typecheck
  ```

- [ ] Run the full Vitest suite:

  ```powershell
  npm test
  ```

  Expected: 318/319 or better. If the only failure is the previously observed `local-supabase-replay` timeout, rerun that exact test file once and record both outcomes; any other failure stops S0.

- [ ] Build the web app:

  ```powershell
  npm run build
  ```

- [ ] Run Android unit tests and assemble a debug package:

  ```powershell
  .\android\gradlew.bat -p .\android testDebugUnitTest assembleDebug
  ```

- [ ] Verify tracked files remain clean after generated outputs are ignored:

  ```powershell
  git status --short
  ```

Expected: typecheck, web build, Android unit tests, and debug assembly pass; no unexpected tracked-file modifications.

## Task 5: Capture the raw local database-test evidence

**Files:**

- Create temporarily outside git: `%TEMP%\kc-s0-db-test.txt`

- [ ] Confirm the Supabase CLI version:

  ```powershell
  npm exec --package=supabase@2.109.1 -- supabase --version
  ```

- [ ] Confirm the local stack is available; start it locally if needed:

  ```powershell
  npm exec --package=supabase@2.109.1 -- supabase status
  ```

- [ ] Run the complete local pgTAP suite and capture stdout/stderr without suppressing its exit code:

  ```powershell
  npm exec --package=supabase@2.109.1 -- supabase test db *>&1 | Tee-Object -FilePath (Join-Path $env:TEMP 'kc-s0-db-test.txt')
  $dbExit = $LASTEXITCODE
  Write-Output "DB_TEST_EXIT=$dbExit"
  ```

- [ ] Record the exact totals and failing test files in the guarded task evidence.

Expected baseline: approximately 26 files / 696 tests with 17 failing files. A materially different count is not silently accepted; Codex inspects it before delegation.

## Task 6: Delegate the source inventory to agy

**Files:**

- Create: `docs/stabilization/s0-source-inventory.md`

- [ ] Install the locked agy order through `Coordination/Commands/Execution Dispatch Command.md` and invoke it with `brain-task.mjs invoke-agy`.
- [ ] Require the report to contain:
  - exact starting refs, merge base, merge commit, and current `HEAD`;
  - complete main-only and Claude-only commit lists;
  - grouped changed-file inventory, highlighting active migrations versus archive;
  - proof that every commit reachable from the recorded `S0_BASE` plus all six Claude-only commits is preserved as an ancestor;
  - explicit statement that no production/release operation occurred;
  - redaction confirmation.
- [ ] Reject any edits outside the one report file.
- [ ] Codex checks every SHA and file count against git before accepting the executor result.

Expected: one accurate, concise Markdown report; no other diff.

## Task 7: Delegate database-failure classification to DeepSeek V4

**Files:**

- Create: `docs/stabilization/s0-db-test-baseline.md`

- [ ] Install the locked V4 order and invoke it with `brain-task.mjs invoke-deepseek-coder`, using the captured local test output and relevant migrations/tests as evidence.
- [ ] Require one row per failing test file with:
  - failing assertion/error signature;
  - classification: `obsolete expectation`, `probable product defect`, `test isolation/flakiness`, or `needs Manager decision`;
  - supporting migration/function/test paths;
  - confidence and the smallest recommended S2 follow-up;
  - no proposed production action and no code changes.
- [ ] Require V4 to distinguish historical ADR-22/shadow expectations from ACL, mute, null-handling, or schema defects; uncertainty must remain explicit.
- [ ] Reject edits outside the one report file.
- [ ] Codex samples every classification against the referenced SQL and corrects only through a new locked executor revision or a clearly signed Manager review section.

Expected: one evidence-linked triage report that does not claim the database suite is green.

## Task 8: Commit the S0 evidence artifacts

**Files:**

- Add: `docs/stabilization/s0-source-inventory.md`
- Add: `docs/stabilization/s0-db-test-baseline.md`

- [ ] Scan both reports for placeholders, leaked identifiers, and unsupported claims:

  ```powershell
  rg -n "TODO|TBD|FIXME|@|token|password|secret" docs/stabilization/s0-*.md
  ```

- [ ] Re-run the ancestry checks from Task 3 and compare report values.
- [ ] Stage only the two reports and commit:

  ```powershell
  git add docs/stabilization/s0-source-inventory.md docs/stabilization/s0-db-test-baseline.md
  git diff --cached --check
  git commit -m 'docs: record S0 source and database baselines'
  ```

Expected: one evidence commit; no code, test, migration, or release file is included.

## Task 9: Align current Brain truth

**Brain files:** limited to the Task 1 write set

- [ ] Update current status and project home to point to the isolated S0 branch/HEAD and distinguish deployed production DB state from unshipped client binaries.
- [ ] Reconcile the five business/platform notes with ADR-0039 and observed runtime truth:
  - `Concern` is alert lifecycle state only and cannot originate an alert;
  - routine learning accepts only coverage-valid healthy intervals, with two independent comparable samples after a long gap;
  - protection outage is visible and recoverable, with one prompt plus persistent `Limited` state;
  - ordinary group status visibility is separate from private default-off Special Attention outage notifications;
  - Guardian/Ward is the broadest ongoing care relationship but cannot impersonate identity, own activity, device consent, or device unlock;
  - AAB/Tauri/iOS Native are target products; APK/PWA are transitional; Play/App Store review constraints are design inputs.
- [ ] Update Known Issues from V4's reviewed classifications without converting uncertainty into fact.
- [ ] Append a signed terse Dev Log entry with merge SHA, verification totals, known red DB baseline, and zero-production-change statement.
- [ ] Run Brain runtime check.

Expected: Brain notes describe one current truth and preserve historical decisions/evidence rather than rewriting them.

## Task 10: Final S0 verification and handoff

**Files:** none expected

- [ ] Run final repository checks:

  ```powershell
  git status --short
  git log --oneline --decorate --graph -n 15
  git diff main...HEAD --check
  ```

- [ ] Re-run `npm run typecheck`, `npm run build`, and the Android command from Task 4 if either report review caused any tracked non-document change. Normally none should exist.
- [ ] Verify the final branch contains the merge commit and evidence commit, while root `main` remains at the recorded `S0_BASE` and clean.
- [ ] Verify there is no new tag, remote push, deployment, version bump, release artifact commit, or linked database mutation.
- [ ] Attach evidence to the guarded task, run the final runtime check, and finish the task only if every S0 acceptance condition is met.
- [ ] Handoff S1 as a new guarded plan: product-semantics and platform-contract test specification. Do not begin S1 implicitly.

Expected: `codex/kc-stabilization-s0` is the sole converged local baseline; online KC continues operating unchanged; S1 has a precise starting SHA and the database defects remain honestly tracked for S2.
